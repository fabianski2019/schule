import tkinter as tk

def button_click(symbol):
    current.text= entry.get
    entry.delete(0, tk.END)
    entry.insert(tk.END, current.text + str(symbol))

def clear.entry
    entry.delete(0. tk.END)